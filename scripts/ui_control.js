
function createNewNewsite(url)
{
	const site = document.createElement("div");
	site.classList.add("newsite-div");

	const schming = document.createElement("p");
	schming.innerHTML = "testtintgg";
	site.appendChild(schming);

	return site;
}
